import 'package:flutter/material.dart';

class MsgIndexPage extends StatelessWidget {
  const MsgIndexPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("msg"),
    );
  }
}
